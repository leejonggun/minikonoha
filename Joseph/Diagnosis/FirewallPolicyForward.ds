// written by Joseph

import("Syntax.ShellStyle");
import("Type.Json");

boolean FirewallPolicyForward(Json ctx) {
	boolean ret = true;

	String ifconfig = $(sudo iptables -L | grep "FORWARD");
	if (ifconfig == "") {
		return false;
	}
	if (ifconfig.search("DROP") > -1) {
		ret = false;
	} else if (ifconfig.search("REJECT") > -1) {
		ret = false;
	}

	return ret;
}

void main() {
	Json ctx = new Json();
	System.p(FirewallPolicyForward(ctx));
}

main();
